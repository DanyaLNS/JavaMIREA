/*public class Main {
    public static void main(String[] args) {
        // Проверка классов
        
        Circle fCircle = new Circle(5);
        System.out.println(fCircle.toString());

        Square fSquare = new Square(3);
        System.out.println(fSquare.toString());

        Rectangle fRectangle = new Rectangle(5, 4);
        System.out.println(fRectangle.toString());

        // Проверка четвертого задания
        MovablePoint fPoint = new MovablePoint(12, 3, 1, 2);
        fPoint.moveLeft();
        fPoint.moveUp();
        System.out.println(fPoint.toString()); 

        MovableCircle sCircle = new MovableCircle(2, 7, 10, 5, 3);
        sCircle.moveRight();
        sCircle.moveDown();
        System.out.println(sCircle.toString());

        // Проверка пятого задания 
        MovableRectangle sRectangle = new MovableRectangle(0, 5, 0, 3, 10, 20);
        sRectangle.moveRight();
        sRectangle.moveUp();
        if(sRectangle.speedCheck()){
            System.out.println(sRectangle.toString());
        }
    }
}
*/